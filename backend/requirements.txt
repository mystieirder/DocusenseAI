# ─────────────────────────────────────────────────────────────────────
#  DocuSense AI — backend (production stack per SRS)
#  API + Celery worker share this dependency set.
# ─────────────────────────────────────────────────────────────────────

# Web framework / server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.3.0
email-validator>=2.1.0

# Database (PostgreSQL + pgvector)
SQLAlchemy>=2.0.30
pgvector>=0.3.0
psycopg[binary]>=3.1.19

# Auth (JWT / OAuth2 / RBAC)
python-jose[cryptography]>=3.3.0
bcrypt==4.0.1
passlib[bcrypt]>=1.7.4

# Async ingestion
celery>=5.4.0
redis>=5.0.0

# Object storage (S3 / MinIO)
boto3>=1.34.0

# Document parsing, OCR, retrieval
PyMuPDF>=1.24.0
python-docx>=1.1.0
pillow>=10.3.0
pytesseract>=0.3.10
numpy>=1.26.0            # vector math: hybrid RRF + L2-normalize for Gemini embeddings

# NOTE: the LOCAL embedding backend (EMBED_BACKEND=local) additionally needs
# sentence-transformers + torch — see requirements-local.txt. The slim cloud image
# (EMBED_BACKEND=gemini) omits them so it fits a 512 MB free instance.

# LLM + summary export
httpx>=0.27.0
reportlab>=4.2.0
